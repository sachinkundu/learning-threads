{
  "name": "learning-threads",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "python3 web/build.py",
    "test": "node --test tests/*.test.cjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "tsc",
    "test:server": "python3 -m unittest discover -s tests -p 'test_*.py'"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260914.1",
    "typescript": "^7.0.2",
    "wrangler": "^4.131.2"
  },
  "dependencies": {
    "jose": "^6.2.12"
  }
}
